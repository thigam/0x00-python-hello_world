-- Lists all records of the table second_table with scores higher than or equal to 10, ordered in order of descedning scores
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
